[project]
name = "cybernetic-dao-wind-tunnel"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "numpy>=1.26",
  "pandas>=2.2",
  "pydantic>=2.7",
  "httpx>=0.28",
]

[project.scripts]
app = "vercel_app:app"
